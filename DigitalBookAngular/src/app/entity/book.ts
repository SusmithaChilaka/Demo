export default class Book {

    constructor (public title:string, public author:string, public publisher:string, public date:string,
        public category:string,  public price:number, public content:string) { }
}